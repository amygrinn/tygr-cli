import * as {{Name}}Actions from './{{name}}.actions';
export { {{Name}}Actions };

import * as {{Name}}ClientSelectors from './client/{{name}}.client.selectors';
export { {{Name}}ClientSelectors };

import * as {{Name}}ServerSelectors from './server/{{name}}.server.selectors';
export { {{Name}}ServerSelectors };

export { {{Name}}Module } from './client/{{name}}.module';

export { {{Name}}Component } from './client/{{name}}.component';

export { {{Name}}Config } from './{{name}}.config';

export { {{name}}ServerConfig } from './server/{{name}}.server.config';