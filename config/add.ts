import * as Shell from 'shelljs';

import { Program } from '../program';

export class Add extends Program {
  name = 'add';
  exec(options: string[]) {
    switch (options[0]) {
      case 'feature_dir':
    }
  }
}