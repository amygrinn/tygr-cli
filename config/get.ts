import * as Shell from 'shelljs';

import { Program } from '../program';

export class Get extends Program {
  name = 'get';
  exec(options: string[]) {
    switch (options[0]) {
      case 'feature_dir':
    }
  }
}