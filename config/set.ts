import * as Shell from 'shelljs';

import { Program } from '../program';

export class Set extends Program {
  name = 'set';
  exec(options: string[]) {
    switch (options[0]) {
      case 'feature_dir':
    }
  }
}